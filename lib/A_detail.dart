import 'package:flutter/material.dart';

class ScreenADetail extends StatelessWidget {
  const ScreenADetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Screen A Detailllll'));
  }
}
